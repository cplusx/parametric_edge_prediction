trainer:
  default_root_dir: outputs/parametric_edge_training/overfit_diverse16_2000_addback_dn_proposal_curve_legacy_compare

model:
  dn_num_groups: 4
  dn_noise_scale: 0.04

loss:
  control_cost: 5.0
  sample_cost: 2.0
  box_cost: 1.0
  giou_cost: 0.0
  curve_distance_cost: 0.0
  dn_weight: 1.0
  dn_curve_weight: 5.0

logging:
  wandb:
    name: overfit-diverse16-2000-addback-dn-proposal-curve-legacy-compare
    tags:
      - overfit
      - gpu
      - diverse16
      - 2000epoch
      - addback
      - dn
      - proposal-curve
      - legacy-compare
    save_dir: outputs/parametric_edge_training/overfit_diverse16_2000_addback_dn_proposal_curve_legacy_compare/wandb